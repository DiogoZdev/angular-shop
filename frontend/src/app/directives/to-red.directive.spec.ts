import { ToRedDirective } from './to-red.directive';

describe('ToRedDirective', () => {
  it('should create an instance', () => {
    const directive = new ToRedDirective();
    expect(directive).toBeTruthy();
  });
});
